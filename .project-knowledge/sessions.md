# Session Log

> Part of job-hunter-agent/.project-knowledge/ | Last updated: 2026-08-16
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-07-20 | Initialized `.project-knowledge/` for this pre-existing project (CREATE mode — no prior `PROJECT_KNOWLEDGE.md`). Scanned and documented the existing 4-step AI job pipeline (agent.py + server.py + ui/index.html), its JSON-file "schema", routes, and systems. No code changes made. |
| 2026-07-20 → 2026-07-21 | First real end-to-end test run with the candidate's actual resume — fixed a locally-broken `claude` CLI install (native binary missing, recurred ~4x this session due to an unrelated auto-updater flakiness on this machine) and set up `.venv`/`.env` (`FIRECRAWL_API_KEY`, `EXA_API_KEY`). Added Exa as a fallback for pages Firecrawl can't scrape (LinkedIn/Reddit). Reworked the dashboard substantially: resume-driven role selection ("Find Roles" → checkboxes), free-text-via-chips run preferences (employment type/pay/location) folded into search + scoring, job source badges, list/grid view toggle with bottom-pinned action buttons, a step-based UI flow (setup → running → results, with Cancel), and a "dodge the red flags" canvas mini-game shown while the pipeline runs. Fixed `run_claude()` to surface `claude` CLI exec failures as a readable error instead of a raw JSON-parse crash in the UI. 40 tests passing (up from 29). |
| 2026-08-16 | No repo code changed — design + toolchain exploration only. Audited MadsLorentzen/ai-job-search (MIT-licensed, 31.8k★ Claude Code job-application framework) and identified what it has that this project lacks: a drafter-reviewer apply loop, CV generation, an application tracker, and PDF/ATS verification. Sketched an adapted apply-flow design (3 new prompt files, `POST /api/apply` SSE endpoint, `output/applications.csv` tracker + archived postings, grounding audit, requirement coverage, language gate) and committed to building CV generation + PDF-compile + ATS verification as our own implementation. Test-drove LaTeX vs Typst for CVs: installed Typst (→ `~/.local/bin`) and portable TinyTeX (→ `~/.TinyTeX`) without sudo, copied Font Awesome 5 fonts to `~/.local/share/fonts`, and styled a Typst CV with section rules + FA icons — using PNG pixel analysis (numpy/PIL) to catch Typst's phantom `par.spacing` gaps. Findings logged in history.md; toolchain decision pending user's render comparison. |
| 2026-08-16 | Still no repo code changed — continued the Typst CV demo (`/tmp/opencode/cv-demo/cv.typ`). Fixed the two remaining layout complaints: oversized gaps around each section rule (cause: bare `line()` is inline, so the rule formed its own paragraph and inherited its line-box height + leading — fixed with `block(spacing: 0pt, line(...))`) and cramped bullet spacing (cause: `list.spacing` is ignored by tight lists — fixed with `tight: false`). Render now fits one page with even spacing. Gotchas appended to history.md. **User approved the render — Typst is the CV toolchain, LaTeX dropped.** Template moved out of `/tmp` into the repo at `templates/cv.typ`. Then **built the CV stage for real**: `prompts/cv.md`, `generate_cvs()`/`_compile_cv()`/`_verify_cv()` in `agent.py` wired as pipeline step 5, `GET /api/cv` serving the PDF, a CV link on each job card, and a 5th step in the UI progress bar. Verified end-to-end against a synthetic non-developer resume (virtual assistant) in a scratch dir — one-page PDF, clean ATS text layer. Two fixes came out of that run: the template's Skills `grid` extracted column-by-column (all labels, then all values) so it became one line per category, and Typst's `~` non-breaking space silently ate "~120 invoices" so the prompt now lists it among the characters to escape. 48 tests passing (up from 40). Then took the rest of the roadmap to done in the same session: language gate in `prompts/analyze.md`, keyword-coverage reporting + a one-page retry for CVs, the **drafter-reviewer apply stage** (`prompts/apply_draft.md`/`apply_review.md`, `apply_to_job()`, `GET /api/apply` SSE, side-by-side modal), and the **application tracker + posting archive** (`output/applications.csv` + `output/applications/<slug>/`). Driving the apply stage through a real browser exposed that `analyze.md` never carries `description` into `jobs.json` — CVs and reviews had been running blind to the posting text — fixed with `_scraped_description()`. Verified end-to-end via the running server: reviewer caught a fabricated "four years" and an Airtable overclaim, coverage went from 1 requirement to 7 once the posting text was restored. 72 tests passing. Then ran it for real against the user's actual resume (`resume.md`, gitignored): role/skill extraction and query building are genuine Claude output; a real posting (n8n Community, $1,500/mo remote n8n AI automation developer) was fetched by hand via WebFetch to stand in for step 2, since no `FIRECRAWL_API_KEY` exists on this machine. Scoring gave it 88/apply with accurate reasons; the CV came out one page with a clean text layer; the reviewer caught "I'm available remote, full-time" as unsupported (the resume's current role is explicitly Part-Time) and cut it. The real run also exposed that the CV keyword check was reporting 59 junk terms — narrowed to posting terms the resume supports but the CV omitted, which drops it to one. 73 tests passing. Finally ran the **whole pipeline for real** once the Firecrawl/Exa keys were available: 9 queries → 73 pages discovered → 20 scraped → scored → letters + CVs. First real run produced **0 jobs above threshold**, which turned out to be a genuine defect rather than a bad market: `EXTRACT_PROMPT` asked for "a short description" and Firecrawl returned a median of 119 characters per posting, so Claude was scoring against blurbs. Rewrote it to demand the posting's own text; re-ran: 168 postings, 20 kept, **10 above threshold**, 10 one-page CVs with clean text layers, 10 cover letters. Also caught (before committing) that real API keys had been put into the tracked `.env.example` — never committed, moved to `.env` by the user. 73 tests passing. Comparing the two real runs then surfaced a bigger problem than the extract prompt: the n8n Community hiring post that scored 88 in run 1 was **absent from run 2 entirely** — discovery finds 46–168 pages, only 20 get scraped, and which 20 is luck of the draw; separately one careers page contributed 30 of 168 postings and half the apply verdicts. Fixed both on the user's own suggestion of a "next page" model: every discovered page is now persisted to `output/page_queue.json`, a **Find more jobs** button scrapes the next batch (never repeating a page, no new search), and `MAX_PAGES_PER_DOMAIN = 3` stops any one site dominating a batch. Cover letters and CVs already written are skipped so a second pass doesn't re-pay for them. Verified against real discovery with scraping stubbed out (zero scrape spend): batch 1 spread across 12 domains instead of 3, batch 2 overlapped batch 1 by zero pages. 84 tests passing. |
| 2026-08-26 | UI/UX audit and redesign. Replaced the generic Tailwind grey/blue palette with a token-based design system (CSS custom properties, light + dark, a persisted toggle), Inter + Newsreader typography, and a rebuilt job card that leads with the score and puts Apply as the one primary action. Audit turned up a real bug: job data interpolated into `onclick` attributes meant an apostrophe in a company name ("Lowe's") broke every button on that card — now delegated via `data-*`. Also fixed the score badge using 80/60 thresholds against the pipeline's 70, the Apply verdict colliding with the Apply button, all disclosures rendering open, and a complete absence of focus styles. Screenshots regenerated for both themes. 84 tests still passing. | Follow-up pass on four issues found by using it: uneven grid card heights (`items-start`), no fade on theme switch (custom properties can't transition — the rule has to target the consuming elements), no way back to setup without paying for a fresh resume analysis (now cached, with explicit back and re-read controls), and action buttons whose labels didn't say what they did. | Then, prompted by the user finding the apply modal confusing, discovered and fixed a genuine design flaw: **two unrelated cover letters per job**. Removed pipeline step 4 entirely so letters are written only on demand by the apply stage, pointed `/api/cover-letter` at the fact-checked letter, and rewrote the modal copy to explain the two-Claude mechanism in plain words. Also rewrote the letter prompts for plain English: no em dashes, short sentences, no stock phrases, with a regex backstop. Verified on a real job — the new letter names an honest gap in four short paragraphs with zero dashes. 84 tests passing. | Finally merged the two per-card letter buttons into one **Cover letter** button with a three-state modal (nothing yet / writing / the letter plus a collapsed "what was checked"), extended `/api/cover-letter` to return the draft and review so those checks survive across sessions, and renamed **CV (PDF)** to **Tailored CV**. Also found the running uvicorn had been serving pre-edit code all along, and a duplicate `renderReview` left by an earlier edit that would have shadowed the new one. |
| 2026-08-27 | Rewrote the README in plain English with no em dashes, documenting the features added over the previous two sessions: the single Cover letter button and its three-state modal, Tailored CV, Find more vs New search (including that a new search discards the queue), the preference chips, the writing-style rules, and where every output file lands. Regenerated both theme screenshots to match the new button labels. Added the project-knowledge attribution block. Also confirmed for the user that CVs really are tailored per posting, with evidence from two CVs off the same resume, and logged the fact that nothing in the UI makes that visible. |
